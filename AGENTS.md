# FrozenProjects project rules

## 原文优先归档

- 聊天、理论、编年史与查漏笔记以项目原始 JSONL/ZIP 为唯一正文来源。
- `extract_fragments.py`、`candidates.json`、聚类、相似度和关键词结果只用于定位候选消息；最终正文必须回到原始消息 ID 重新读取。
- 正文逐字保留原文：换行、标点、大小写、拼写、语言、表情标记、回复标记、发送者名称与时间均按源数据写入。
- 只生成标题或小标题作为摘要。正文不写 agent 自己的概括、推理链、理论发展路径、证据判断或结论。
- 需要统计、聚类和审计时，将其放进 sidecar JSON 或独立机器报告；原文档案正文只放源文本。
- 按 `sender.uin` 和原始 message ID 取数；不按昵称替代 UIN，不因改名合并不同来源。
- 转发、嵌套消息、回复消息各自保留来源层级；图片、音频等非文字内容保留导出器原始标记或资源引用。

## 交付检查

- 每个引文块都能回到原始 JSONL 的 message ID。
- 引文正文与原始 `body/text` 做逐字比对；候选预览与原文出现差异时，以原始 JSONL 为准。
- 笔记中除标题外不存在新写的摘要句或解释段。
- 所有路径、回链和 sidecar 文件均实际存在。
